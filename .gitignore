# Rust
/target
**/*.rs.bk
Cargo.lock.orig

# Render output
/out
*.png
# ...except the committed README gallery
!docs/renders/*.png

# Editor / OS
.DS_Store
*.swp

# prime-cuda has its own (excluded) build dir
/crates/prime-cuda/target
